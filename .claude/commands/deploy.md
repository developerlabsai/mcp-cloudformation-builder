# Deploy to Vercel

Deploy the current branch to Vercel.

## Steps:
1. Run `npm run build` to verify build succeeds
2. Run `vercel --prod` to deploy to production
3. Report the deployment URL

If build fails, analyze the errors and suggest fixes.
